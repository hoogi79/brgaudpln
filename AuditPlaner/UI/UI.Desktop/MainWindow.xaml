<Window x:Class="bregau.AuditPlaner.UI.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:bregau.AuditPlaner.UI.Desktop"
        mc:Ignorable="d"
        Title="{Binding DynamicWindowTitle, Mode=OneWay}"
        DataContext="{Binding Main, Source={StaticResource Locator}}" Height="426.737" Width="692.623">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="8*"/>
            <ColumnDefinition Width="11*"/>
        </Grid.ColumnDefinitions>
        <ProgressBar HorizontalAlignment="Left" Height="30" Margin="10,367,0,0" VerticalAlignment="Top" Width="664" Value="{Binding Progress}" Grid.ColumnSpan="2"/>

    </Grid>
</Window>
